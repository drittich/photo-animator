<Application x:Class="PhotoAnimator.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PhotoAnimator.App"
             xmlns:sys="clr-namespace:System;assembly=mscorlib">
    <Application.Resources>
        <!-- Icon toolbar resources -->
        <SolidColorBrush x:Key="ToolbarIconForegroundBrush" Color="{DynamicResource {x:Static SystemColors.ControlTextColorKey}}" />
        <SolidColorBrush x:Key="ToolbarIconForegroundDisabledBrush" Color="#66000000" />
        <SolidColorBrush x:Key="ToolbarIconForegroundActiveBrush" Color="{DynamicResource {x:Static SystemColors.HighlightColorKey}}" />
        <SolidColorBrush x:Key="ToolbarIconBackgroundCheckedBrush" Color="#22007ACC" />
        <Thickness x:Key="ToolbarIconPadding">6,6,6,6</Thickness>
        <sys:Double x:Key="ToolbarIconFontSize">16</sys:Double>

        <Style x:Key="ToolbarIconButtonStyle" TargetType="Button">
          <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
          <Setter Property="FontSize" Value="{StaticResource ToolbarIconFontSize}" />
          <Setter Property="Padding" Value="{StaticResource ToolbarIconPadding}" />
          <Setter Property="Width" Value="32" />
          <Setter Property="Height" Value="32" />
          <Setter Property="Background" Value="Transparent" />
          <Setter Property="BorderThickness" Value="0" />
          <Setter Property="Foreground" Value="{StaticResource ToolbarIconForegroundBrush}" />
          <Setter Property="HorizontalContentAlignment" Value="Center" />
          <Setter Property="VerticalContentAlignment" Value="Center" />
          <Setter Property="ToolTipService.ShowOnDisabled" Value="True" />
          <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Background" Value="#11000000" />
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
              <Setter Property="Background" Value="#22000000" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{StaticResource ToolbarIconForegroundDisabledBrush}" />
            </Trigger>
          </Style.Triggers>
        </Style>

        <Style x:Key="ToolbarIconToggleButtonStyle" TargetType="ToggleButton">
          <Setter Property="FontFamily" Value="Segoe MDL2 Assets" />
          <Setter Property="FontSize" Value="{StaticResource ToolbarIconFontSize}" />
          <Setter Property="Padding" Value="{StaticResource ToolbarIconPadding}" />
          <Setter Property="Width" Value="32" />
          <Setter Property="Height" Value="32" />
          <Setter Property="Background" Value="Transparent" />
          <Setter Property="BorderThickness" Value="0" />
          <Setter Property="Foreground" Value="{StaticResource ToolbarIconForegroundBrush}" />
          <Setter Property="HorizontalContentAlignment" Value="Center" />
          <Setter Property="VerticalContentAlignment" Value="Center" />
          <Setter Property="ToolTipService.ShowOnDisabled" Value="True" />
          <Style.Triggers>
            <Trigger Property="IsChecked" Value="True">
              <Setter Property="Background" Value="{StaticResource ToolbarIconBackgroundCheckedBrush}" />
              <Setter Property="Foreground" Value="{StaticResource ToolbarIconForegroundActiveBrush}" />
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter Property="Background" Value="#11000000" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Foreground" Value="{StaticResource ToolbarIconForegroundDisabledBrush}" />
            </Trigger>
          </Style.Triggers>
        </Style>
    </Application.Resources>
</Application>
